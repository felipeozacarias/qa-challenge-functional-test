# Validação Local do CI — 2026-08-19

Validação realizada no Windows utilizando Git Bash após a evolução do projeto com execução Newman via npm e geração de relatório JUnit.

## Ambiente

```text
Shell: Git Bash
Execução: Newman via npm
Relatório: JUnit XML
```

## Comando executado

```bash
npm run ci
```

O script executado pelo projeto foi:

```bash
node -e "require('fs').mkdirSync('reports',{recursive:true})" && newman run postman/Advantage_Search_API_Postman_Collection_v2.json --reporters cli,junit --reporter-junit-export reports/newman-report.xml
```

## Resultado

```text
Iterations: 1 executada / 0 falhas
Requests: 3 executadas / 0 falhas
Test scripts: 3 executados / 0 falhas
Assertions: 8 executadas / 0 falhas
Total run duration: 3.4s
Average response time: 1018ms
Exit code: 0
```

Requests validadas:

```text
GET Search Products - Valid Term: 200 OK
GET Search Products - Invalid Term: 200 OK
GET Search Products - Missing Query Parameter: 500 Internal Server Error observado e tratado como cenário negativo
```

## Relatório gerado

```text
reports/newman-report.xml
```

O arquivo JUnit foi gerado com sucesso e pode ser consumido por pipelines e ferramentas de integração contínua.

## Conclusão

A execução confirmou que:

- a collection continua funcional após a evolução do projeto;
- as 3 requests foram executadas sem falhas de execução;
- as 8 assertions foram aprovadas;
- o cenário sem parâmetro preserva o comportamento HTTP 500 observado;
- o comando `npm run ci` é reproduzível localmente no Git Bash;
- o relatório JUnit é gerado corretamente;
- a execução finaliza com exit code `0`.

Esta evidência complementa os registros anteriores e representa a validação da versão evoluída do projeto funcional.
